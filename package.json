{
  "name": "copilot-usage-insights",
  "version": "1.0.0",
  "description": "GitHub Copilot Usage Insights Dashboard",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "cd api && npm run dev",
    "dev:web": "cd web && npm run dev",
    "dev:team-report": "cd team-report-app && npm run dev",
    "dev:all": "concurrently \"npm run dev:api\" \"npm run dev:web\" \"npm run dev:team-report\"",
    "build": "npm run build:api && npm run build:web",
    "build:api": "cd api && npm run build",
    "build:web": "cd web && npm run build",
    "build:team-report": "cd team-report-app && npm run build",
    "install:all": "npm install && cd api && npm install && cd ../web && npm install && cd ../team-report-app && npm install",
    "db:seed": "cd api && npm run db:seed",
    "test": "cd api && npm test",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "concurrently": "^8.2.2",
    "puppeteer": "^24.36.1"
  }
}
